const SectionTitle = ({ title }) => {
  return (
    <div className="flex justify-center mt-10">
      <h1 className="text-4xl font-bold italic text-center uppercase">
        {title}
      </h1>
    </div>
  );
};

export default SectionTitle;
